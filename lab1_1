.model small
.stack
.data
Message db 'Hello world!', 13, 10, '$'
.code
begin:
    mov ax, @data
    mov ds, ax

    mov ah, 9
    mov dx, offset Message
    int 21h

    mov ah, 4ch
    int 21h
end begin
